# Artifact Manifest

- Artifact: `p-n-p-main_BASELINE_07-24-26_<sha12>.zip`
- Repo: `p-n-p-main`
- Packaged root: `p-n-p-main`
- Mode: full baseline snapshot
- Date: 2026-07-24
- Source basis: uploaded `p-n-p-main.zip`
- Canonical release validation: `npm run validate:release`
- Packaging validation: ZIP must be reopened and canonical validators rerun from extracted contents
- Updater target: generic v3.1 snapshot updater
- Status: STRUCTURALLY CHECKED — LOCAL VALIDATION REQUIRED

## 2026-07-24 authority-scale modernization

- Added compressed deterministic 100K opportunity runway with 20 gzip JSONL shards.
- Added 2,500-opportunity rotating operational window and 250-item candidate backlog.
- Added improve-before-new-URL scoring and deterministic Twin-learning transfer improvement plans without installing Twin Agent.
- Added truthful citation/KPI contract, evidence ledger, scoreboard, and 100K/180-day stretch objective.
- Added evidence-gated publication velocity: `3 → 5 → 10 → 15 → 25 → 50` as ceilings, never quotas.
- Added public authority admission and accepted-output freeze governance.
- Added four-territory authority health proof for porch, party, hotel-room, and grazing surfaces.
- Added validated post-publish distribution preparation for IndexNow and Google Search Console with durable workflow evidence.
- Replaced redundant weekly/IndexNow schedules with a single governed daily authority cycle plus canonical distribution workflow.
- Added full-cycle determinism/idempotency and hostile freeze/velocity proofs during artifact construction.

## 2026-08-07 closed-loop search intelligence

- Added a search-intelligence lane at `scripts/search_intelligence/` and `data/search_intelligence/` implementing target query set → live grounded observation → competitor comparison → real Google Search Console truth → diagnosis → smallest bounded repair candidate → existing approval/publishing rules → same-query retest → before/after evidence → authority feedback.
- Added `data/search_intelligence/search_intelligence_contract.json` encoding five machine-checked hard rules.
- Added `distribution_scripts/gsc_search_analytics.py`, a read-only Search Analytics collector reusing the existing GSC service-account pattern.
- Added `npm run validate:search-intelligence` as a hard-fail member of `npm run validate:release`.
- Added 15 hostile fixtures (`npm run search:selftest`) proving the validator fails on rank conflation, silent green, non-GSC own-site claims, allowance breach, cadence capture, and illegal verified-citation promotion.
- Added a 24-check end-to-end loop proof (`npm run search:selftest:loop`) driving the full loop with fixture provider evidence in an isolated sandbox.
- Added `.github/workflows/search-intelligence.yml` as manual dispatch only, so real provider collection never becomes a scheduled cadence change.
- Extended `data/ops/pnp_local_authority_contract.json` with the search-intelligence lane, its writable paths, and its hard-fail conditions.
- Preserved the existing authority engine unchanged: `authority:cycle`, the publish queue, admission, freeze, velocity governor, sitemap, and the distribution lane were not modified.

## Truth boundaries

- Twin Agent is not installed. Twin remains authorized only for Velocity and Spry.
- 100K fanout records are planning intelligence, not 100K pages or citations.
- Verified external citation count remains zero until evidence is recorded.
- No new geography outside `data/service_areas/areas.json` may be admitted.
- No fake reviews, customer projects, rankings, citations, pricing, or local presence may be generated.
- Grounded/generative search observation is never a literal Google SERP rank.
- Google Search Console is the only authority for own-site Google impressions, clicks, CTR, average position, and indexation.
- Provider unavailable/degraded state is reported honestly and never rendered as green or OK.
- A passing search-intelligence validator proves contract compliance only; it does not prove any provider was reachable.
- Search intelligence prepares repairs. It never publishes and never changes the publishing cadence.
- At packaging time no real provider truth had been collected: grounded search and Google Search Console both reported UNAVAILABLE in the local environment.
